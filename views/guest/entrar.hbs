<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">


<div class="wrapper">
      <h2 class="active">Tempo Restante: <b><div id="countdown"></div></b></h2>
    <input type="hidden" id="endtime" value="{{EndTime}}"></imput> 
  <div id="formContent">
    <!-- Tabs Titles -->

    <h2 class="active"> Votação Assec </h2>
    <!-- Icon -->
    <div class="fadeIn first">
      <img typeclass="img-fluid" alt="Responsive image" src="/assec.png" width="200" alt="">
    </div>
    <br>

    <!-- Login Form -->
    <form >
      <input type="number" id="mat"  name="mat" placeholder="Matrícula" onkeydown = "if (event.keyCode == 13) join()" required>
      <br>
      <input  type="reset" class="fadeIn fourth" value="Entrar" onclick="join()" >
    </form>

    <!-- Remind Passowrd -->
    <div id="formFooter">
      <a class="underlineHover" href="https://api.whatsapp.com/send?phone=5584988119395&text=N%C3%A3o%20estou%20conseguindo%20votar.%20">Problemas de acesso?</a>
    </div>

  </div>
</div>

<script>

  function countDownTimer() {
  const difference = document.getElementById('endtime').value - +new Date();

  let remaining = "Acabou o tempo!";

  if (difference > 0) {
    const parts = {
      h: Math.floor((difference / (1000 * 60 * 60)) % 24),
      min: Math.floor((difference / 1000 / 60) % 60),
      s: Math.floor((difference / 1000) % 60),
    };
    remaining = Object.keys(parts).map(part => {
    return `${parts[part]} ${part}`;
    }).join(" ");
  }else{
    window.location.href = "/entrar/"
  }

  document.getElementById("countdown").innerHTML = remaining;
}

countDownTimer()
setInterval(countDownTimer, 1000)

</script>

<script>

function join (){

event.preventDefault()

var value = []

value.push({mat: document.getElementById('mat').value})
console.log(value)
if(value){
      $.ajax({
        url:'/entrar',
        method:'POST',
        contentType: 'application/json',
        data: JSON.stringify(value)
      }).done(function(retorno){
     if (retorno.erro == "ncadastrado"){
                Swal.fire({
                  icon: 'question',
                  title: 'Matrícula não cadastrada',
                  text: 'Erro? Entre em contato no link abaixo',
                  showConfirmButton: true,
                  footer: '<a href=https://api.whatsapp.com/send?phone=5584988119395&text=N%C3%A3o%20estou%20conseguindo%20votar.%20>Entrar em contato</a>'
                })
                
     } else if (retorno.erro == "votado"){
               Swal.fire({
                  icon: 'error',
                  title: 'Usuário ja votou',
                  text: 'Obrigado pela participação',
                  showConfirmButton: true,
                })

     } else if (retorno.local){
       
       window.location.href = "/voto/"+ retorno.local+"/"+ retorno.mat;

     }

      })


}

}  

</script>


<style>
  @import url('https://fonts.googleapis.com/css?family=Poppins');

/* BASIC */

html {
  background-color: #d1d1d1;
}

body {
  font-family: "Poppins", sans-serif;
  height: 100vh;
}

a {
  color: #92badd;
  display:inline-block;
  text-decoration: none;
  font-weight: 400;
}

h2 {
  text-align: center;
  font-size: 16px;
  font-weight: 600;
  text-transform: uppercase;
  display:inline-block;
  margin: 40px 8px 10px 8px; 
  color: #cccccc;
}



/* STRUCTURE */

.wrapper {
  display: flex;
  align-items: center;
  flex-direction: column; 
  justify-content: center;
  width: 100%;
  min-height: 100%;
  padding: 20px;
}

#formContent {
  -webkit-border-radius: 10px 10px 10px 10px;
  border-radius: 10px 10px 10px 10px;
  background: #fff;
  padding: 30px;
  width: 90%;
  max-width: 450px;
  position: relative;
  padding: 0px;
  -webkit-box-shadow: 0 30px 60px 0 rgba(0,0,0,0.3);
  box-shadow: 0 30px 60px 0 rgba(0,0,0,0.3);
  text-align: center;
}

#formFooter {
  background-color: #f6f6f6;
  border-top: 1px solid #dce8f1;
  padding: 25px;
  text-align: center;
  -webkit-border-radius: 0 0 10px 10px;
  border-radius: 0 0 10px 10px;
}



/* TABS */

h2.inactive {
  color: #cccccc;
}

h2.active {
  color: #0d0d0d;
  border-bottom: 2px solid #5fbae9;
}



/* FORM TYPOGRAPHY*/

input[type=button], input[type=submit], input[type=reset]  {
  background-color: #56baed;
  border: none;
  color: white;
  padding: 15px 80px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  text-transform: uppercase;
  font-size: 13px;
  -webkit-box-shadow: 0 10px 30px 0 rgba(95,186,233,0.4);
  box-shadow: 0 10px 30px 0 rgba(95,186,233,0.4);
  -webkit-border-radius: 5px 5px 5px 5px;
  border-radius: 5px 5px 5px 5px;
  margin: 5px 20px 40px 20px;
}

input[type=button]:hover, input[type=submit]:hover, input[type=reset]:hover  {
  background-color: #39ace7;
}


input[type=number], input[type=password] {
  background-color: #f6f6f6;
  border: none;
  color: #0d0d0d;
  padding: 15px 32px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 5px;
  width: 85%;
  border: 2px solid #f6f6f6;
  -webkit-border-radius: 5px 5px 5px 5px;
  border-radius: 5px 5px 5px 5px;
}

input[type=text]:focus {
  background-color: #fff;
  border-bottom: 2px solid #5fbae9;
}

input[type=text]:placeholder {
  color: #cccccc;
}

/* Simple CSS3 Fade-in Animation */
.underlineHover:after {
  display: block;
  left: 0;
  bottom: -10px;
  width: 0;
  height: 2px;
  background-color: #56baed;
  content: "";
  transition: width 0.2s;
}

.underlineHover:hover {
  color: #0d0d0d;
}

.underlineHover:hover:after{
  width: 100%;
}


/* OTHERS */

*:focus {
    outline: none;
} 

#icon {
  width:60%;
}

* {
  box-sizing: border-box;
}
</style>